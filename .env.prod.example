# EC2 /home/ubuntu/my-portfolio/.env — reference only (compose managed on server)
# cp .env.prod.example .env

DB_USERNAME=postgres
DB_PASSWORD=change-me-strong-password

# Optional GitHub Actions secrets (nginx 1회 설정 시):
# MIDO_SERVER_NAME=mido.yourdomain.com
# MIDO_SETUP_NGINX=1
